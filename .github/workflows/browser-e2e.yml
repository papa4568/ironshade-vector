name: Browser E2E

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'tests/**'
      - 'scripts/**'
      - 'package.json'
      - 'package-lock.json'
      - '.nvmrc'
      - 'vite.config.ts'
      - 'index.html'
      - '.github/workflows/browser-e2e.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'src/**'
      - 'tests/**'
      - 'scripts/**'
      - 'package.json'
      - 'package-lock.json'
      - '.nvmrc'
      - 'vite.config.ts'
      - 'index.html'
      - '.github/workflows/browser-e2e.yml'

permissions:
  contents: read

jobs:
  browser-e2e:
    strategy:
      fail-fast: false
      matrix:
        viewport:
          - desktop
          - mobile-landscape
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up Node 22
        uses: actions/setup-node@v7
        with:
          node-version: '22.23.2'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Set up stable Chrome
        id: setup-chrome
        uses: browser-actions/setup-chrome@v2

      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund

      - name: Run full regression and production build
        run: npm run build

      - name: Start production preview
        shell: bash
        run: |
          set -euo pipefail
          npm run preview -- --host 127.0.0.1 --port 4173 > browser-preview.log 2>&1 &
          echo $! > "$RUNNER_TEMP/ironshade-preview.pid"
          for attempt in {1..60}; do
            if curl --fail --silent http://127.0.0.1:4173/ >/dev/null; then
              echo 'Production preview is ready.'
              exit 0
            fi
            sleep 0.5
          done
          cat browser-preview.log
          exit 1

      - name: Launch headless Chrome
        shell: bash
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
        run: |
          set -euo pipefail
          "$CHROME_PATH" \
            --headless=new \
            --no-sandbox \
            --disable-dev-shm-usage \
            --remote-debugging-address=127.0.0.1 \
            --remote-debugging-port=9223 \
            --user-data-dir="$RUNNER_TEMP/ironshade-browser-e2e" \
            http://127.0.0.1:4173/ > browser-chrome.log 2>&1 &
          echo $! > "$RUNNER_TEMP/ironshade-chrome.pid"
          for attempt in {1..60}; do
            if curl --fail --silent http://127.0.0.1:9223/json/version >/dev/null; then
              echo 'Headless Chrome CDP endpoint is ready.'
              exit 0
            fi
            sleep 0.5
          done
          cat browser-chrome.log
          exit 1

      - name: Verify browser player journey
        env:
          CDP_ENDPOINT: http://127.0.0.1:9223
          BROWSER_E2E_APP_URL: http://127.0.0.1:4173/
          BROWSER_E2E_VIEWPORT: ${{ matrix.viewport }}
          BROWSER_E2E_SCREENSHOT: browser-e2e-${{ matrix.viewport }}.png
        run: |
          node scripts/browser-runtime-smoke.mjs
          node scripts/verify-authored-operator.mjs
          node scripts/verify-authored-enemies.mjs
          node scripts/verify-authored-weapons.mjs
          node scripts/verify-authored-refinery.mjs

      - name: Upload browser QA evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v7
        with:
          name: ironshade-vector-browser-e2e-${{ matrix.viewport }}
          path: |
            browser-e2e-${{ matrix.viewport }}.png
            browser-preview.log
            browser-chrome.log
          if-no-files-found: warn
          retention-days: 30
